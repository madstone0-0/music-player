const int NESTED_NAV_ID = 1;

/// Approximate intrinsic height of the MiniPlayer card (progress bar + row
/// content + outer padding).  Used to reserve scroll space so list content is
/// not obscured when the mini-player is visible.
const double kMiniPlayerHeight = 80.0;
